# Embedded, file-based H2 database - no MySQL installation needed.
spring.datasource.url=jdbc:h2:file:./data/finwise_db;MODE=MySQL;DATABASE_TO_LOWER=TRUE;AUTO_SERVER=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
