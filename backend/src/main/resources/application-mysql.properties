# Kept for backwards compatibility: MySQL is already the default configuration
# in application.properties. Credentials come from DB_USERNAME / DB_PASSWORD.
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
